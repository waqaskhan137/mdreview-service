# Run: sprint-39

Process: `docs/process/autonomous-run.md`. Parallel run: #288 (this file's first entry), #144,
#267 in separate worktrees; #187 shipped ahead of this entry. One agent per ticket, per-agent
worktrees, contention map per epic #275.

## Decision log

### D1 (#288) — precondition compare lives in ReviewService.put_source, not the PUT arm
**Decided:** the expected-revision compare sits at the top of `ReviewService.put_source`, raising
`ReviewWriteRejected(status=409)`, with the HTTP arm only parsing If-Match / the
`expected_revision` body key into an int. The caller already holds `store.lock` (the method's
documented contract), so the compare is atomic with the snapshot+overwrite, and the latex
decorator (`LatexAwareReviews.put_source`, widened in the same change) inherits the guard through
`self._inner.put_source` with zero duplicated logic.
**Falsified if:** a put_source caller appears that does not hold store.lock (the compare would
race), or a 409 is observed with a partial write (snapshot taken, source not replaced).

### D2 (#288) — MCP transport for the token: body key in, opt-in envelope out
**Decided:** `update_source` sends `expected_revision` as a PUT body key (route() stays a pure
(method, path, body) mapping; an old server drops the key, an old wrapper never sends it), and
`get_source` gains `with_revision=true` returning `{"source", "revision"}` built from ONE
response (body + ETag), never two fetches. The default `get_source` result stays the raw
document verbatim; the epic forbids a default-on envelope as a breaking change.
**Falsified if:** an existing agent's `update_source` body changes byte-wise when the parameter
is omitted (pinned by a test), or the envelope's revision can disagree with the text it
accompanies.

### D3 (#288) — golden-transcript re-bless, scope confirmed by running the oracle
**Decided:** ran `tests/access_seam_oracle.py` (before = origin/dev at df40f3a, after = this
branch). Drift in BOTH tiers is exactly one row each — the live `GET /status` gains
`"can_edit": true` and `"revision": 0` — and nothing else moves (the ETag is invisible to the
transcript, which captures status+body+Content-Type only; the fresh-meta `revision: 0` stamp is
absorbed by `summary()`'s existing default). This is epic #273 named risk 3, accepted at the
owner's G1 sign-off; the oracle's exit 1 against pre-#288 baselines is the accepted, recorded
consequence, and the baseline is re-blessed as: /status carries `status` (#187), `revision` and
`can_edit` (#288) on top of the MR-051 keys. `source_updated_by` drift lands with A2 (#289).
**Falsified if:** a before/after oracle run shows drift in any row other than the live /status
row of each tier.

### D4 (#288) — mid-run rebase instead of park when dev moved
**Decided:** origin/dev moved during implementation (#187 merged, touching `server.py`'s /status
arm — `summary()` + a `status` key — and `reviews.py`; #144 merged, touching pr-checks.yml).
Rule 9 (park, don't clobber) targets unread overwrites; the sibling diffs were read in full and
the branch rebased onto df40f3a: /status now returns #187's `status` PLUS #288's
`revision`/`can_edit`; both siblings' pr-checks steps kept ahead of mine. Every check rerun
post-rebase, including the siblings' own (`manual_resolve_smoke`, `admin_audit_selfcheck`).
**Falsified if:** #187's smoke or the merged /status shape regresses on dev after my merge.
