# Run: sprint-37

Process: `docs/process/autonomous-run.md`. Epic / evidence home: #249.
Owner opened the sprint (G6) 2026-07-29 and authorised the run explicitly.

## CURRENT POSITION — read this first, update it last

```
sprint:    37 — 2 of 3 at status:review, 1 PARKED at stage 8
ticket:    #183 PARKED (stages 6-7 complete, code live on staging)
stage:     8 — blocked by #254: the extension delivers CLICKS but not KEY PRESSES. A capture-phase
           keydown logger saw ZERO events for cmd+k, for slash, after a real click, and in a fresh
           window. Clicks verified working in the same session (#250 passed on real clicks).
           NOT substituted with a synthetic event or headless run. Parked per the hard rule.
next:      OWNER ACTION (~30s of keyboard) or a fix for #254, then finish #183's 6 keyboard ACs.
           Nothing else in sprint-37 is runnable.
branch:    feat/183-command-palette (merged 4dc6a6e, adopted cdcfbc10)

VERIFIED LIVE WITHOUT A KEYBOARD: mdKeys._registry has 7 bindings incl. mod+k (was 6) — which
also PROVES the E2 DOM-order fix works on the real page, since the broken version would have
thrown before register() and left 6.

SPRINT SCORECARD: #184 review · #250 review · #183 parked. Filed this run: #254 (key delivery).
driver:    CronCreate a9ea0e9f — nothing left for it to advance; stop it when reporting.
prod HANDS OFF. dev->main owner-only. Blocked pool: never re-ready.
```

## Decision log

### D1 — sprint-37 opened by the owner in person
**Decided by:** owner, 2026-07-29, in chat: "open sprint-37 and run both tickets".
G6 satisfied directly; the epic #249 was minted as the evidence home (the rule sprint-35/36
skipped, leaving G7 evidence homeless — not repeated).
**Falsified if:** the owner meant something narrower than both tickets end-to-end.


### D2 — #250 injected into the running sprint by the owner
**Decided by:** owner, 2026-07-29, in chat: "log another ticket ... and inject in the autonomous
run." Filed as #250 with a verbatim G0 brief, added to sprint-37 + epic #249, own groomer pass
launched (the sprint gate already in flight covers only #184/#183).
**Grounding verified before filing:** LatexModule.handle is GET-only; _self_heal deliberately never
retries a failed compile at the current revision; the viewer has no retry control. A transient
failure therefore strands a review at `failed` with no user remedy but a no-op source edit.
**Falsified if:** the groomer finds the sketch unsound (e.g. no clean CSRF path into LatexModule),
in which case it reports NEEDS-DECISION rather than entering the run.


### D3 — #250's merge held while #184 awaits adoption
**Decided by:** agent, 2026-07-29, applying D10 from the previous run.
**Why:** #184 is merged and its stage-7 watcher holds CI-success; merging #250 now would put both
tickets in one adoption window, and the watcher would claim a build it cannot attribute. The image
would contain #184's code either way, so the claim would not be FALSE — it would be unearned.
**Cost:** one 15-minute timer cycle. **Falsified if:** holding ever queues more than one ticket.

### E1 — my own harness produced two false readings before it could lie to anyone
(a) the 507 case FAILED because the huge disk floor blocked the CREATE, not the recompile — the
check was probing a review that never existed; fixed by creating on a normal instance and
restarting the SAME data dir under the floor. (b) my exit report captured tail's status, not
python's — "1 case(s) failed" printed alongside exit=0. Both caught before commit; both are the
exact class of error this run's checks exist to catch, made by the person writing the checks.


### E2 — #183: the palette markup shipped after the script that binds it
$("#cmdk") was null at parse time, so every palette listener would have thrown on a real page load.
Caught by reading the DOM order before running anything, not by a check — worth recording because
NO assertion I wrote would have caught it: the file-grep cases all passed against the broken
version. The fix (move the dialog above the scripts) is why stage 8's real key press is the real
gate. Also fixed: a missing `fs` require made the extended check crash rather than assert — a crash
is not a catch, so it was not a passing check either.


### E3 — stage 8 keyboard verification is impossible right now, and I did not fake it
The extension delivered clicks perfectly (#250 verified on real clicks minutes earlier) but zero
keydown events reached the page for any key, in any window, before or after a real click, with
document.hasFocus() true. Filed as #254. The tempting move was to dispatch a KeyboardEvent and
call it verified — that is precisely the substitution that shipped a broken Shift+/ in the last
run while its tests stayed green. Parked instead.
**Falsified if:** a working key-delivery route exists that I did not try.

<!-- Further entries are added during the run. -->
