# Run: sprint-35 then sprint-36

Process: `docs/process/autonomous-run.md`. Driver: `/loop`.
Owner reviews once work is on staging and verified.

## CURRENT POSITION — read this first, update it last

Every `/loop` firing is a fresh context. This block is the only handoff between wakeups.

```
sprint:    36 — LAST TICKET IN FLIGHT
ticket:    #206
stage:     6 done — PR #247 merged as b8e837c. Stage 7 watcher armed.
next:      stage 7 both halves -> stage 8 (API reproduction, NOT the browser: latexguard.py has no
           rendered surface) -> stage 9. THEN THE RUN ENDS.
branch:    fix/206-latexguard-inline-spans     worktree: .scratch/wt-206
pre-merge .deployed-digest: sha256:f2f59dd4195c1260cc7d658240a504b127fbc7a6cc3f2a97861791cac55ebc43

FINAL STATE
  status:review : #216 (af7ccaf) #214 (5f2c2c0) #205 (ef9f524) #213 (edc13be) #224 (748c6e7)
  PARKED        : #199 — merged 6a09903 + deployed, stage 8 incomplete (1 of 3 widths in real
                  Chrome). Owner owes 1280 and 1400. Recipe: NEW window -> resize -> THEN navigate.
  in flight     : #206
  BLOCKED, never re-ready: #236 #235 #113 #215
  sprint-37 (#183, #184) is a CONTAINER THE OWNER MUST OPEN. Do not claim from it.

OWNER OWES, in priority order:
  1. #199's remaining widths (1280, 1400) — ~2 min with the recipe above.
  2. #235 prod TTL (config only, prod image already reads the var) — you are still on 12h.
  3. dev -> main promotion (#236 is stale; the owner already promoted once at 21:15Z).
  4. #113 prod-data reconciliation. 5. sprint-37 activation. 6. #243/#241 disposition.
```

Wakeup protocol: read this block, do the next stage, rewrite this block, commit.

## Order

sprint-35: **236, 235, 113, 216, 214, 215**
sprint-36: **199, 205, 213, 224, 184, 183, 206**

One ticket at a time, in priority order. The groomer gates each sprint before its first ticket.

## Standing constraints for this run

Carried from `docs/process/autonomous-run.md` and from the 2026-07-28 session's own-goals:

- **Stage 7 needs BOTH halves.** CI concluded successfully for *my* merge SHA, AND the staging
  digest moved off the value recorded *before* merging. A digest change while my CI is still
  running is someone else's build: re-baseline and keep waiting. This already caught a false
  positive once (#222 would have been "verified" against an image containing none of its work).
- **An empty digest read is not adoption.** Treat it as a failed read and keep waiting (#216).
- **Stage 8 is the claude-in-chrome extension. Owner direction, 2026-07-28: NO headless, ever.**
  Real key presses, real clicks, real screenshots, and actually LOOK at them.
  There is **no headless fallback**. If the extension is unavailable, the ticket PARKS at stage 8
  with that stated plainly on the issue; it does not get "verified" by a weaker method.
  Why this is a hard rule and not a preference: headless CDP passed the `Shift+/` case while the
  feature was broken, because the check constructed its own KeyboardEvent and could therefore only
  confirm the author's assumption. The extension sends real input and caught it in one press.
  Substituting a local or synthetic check for the real one is hard rule 7's definition of faking a
  verification.
- **A node in the DOM is not a legible node**, and a 200 is not a render.
- **Cite symbols, not line numbers.** Line numbers in this repo rot within days.
- **Read `origin/dev`, never the working tree.** It has been on a stale branch.
- **Never weaken an acceptance criterion to make a ticket green.** That is a finding: file it.
- **Stop at G4.** Swap to `status:review`; never close (G5 is the owner's) and never touch `main`.

## Escalation during this run — the owner is ABSENT

Owner direction, 2026-07-28: **the owner is not in this loop and is represented by the
`product-owner` agent.** Any decision the run cannot make goes to that agent, its answer is
recorded here, and the run proceeds on it. Do not stall waiting for a human.

Every proxy decision is logged like any other: what was decided, why, what would falsify it, and
attributed to **product-owner (owner proxy)** rather than to the owner personally, so a later
reader can tell a delegated call from one the owner made themselves.

### The boundary the proxy cannot cross

Amended by the owner, 2026-07-28, after the first draft was too broad. **The proxy owns every
decision through dev and staging, and additionally owns reversible production configuration.**

Exactly TWO things still stop the run and wait for the owner personally:

1. **`dev -> main` promotion, and anything touching `main`.** The owner's gate, on purpose. G8 and
   hard rule 10. Affects **#236**.
2. **Mutating production DATA.** Affects **#113** (one-time reconciliation of the live prod
   `owner=''` corpus).

Why #113 stays with the owner while #235 does not — the distinction is **reversibility**, not
which environment is touched:

- **#235** adds an env var and recreates a container. Wrong, it is undone by editing one line and
  recreating again. Sessions survive it. The proxy can authorise it.
- **#113** stamps ownership onto real user documents. Wrong, it is not undoable, and it is
  precisely the failure that produced #97: a blind back-fill assigned another user's document to
  the owner. No proxy authority covers an irreversible act on other people's data.

Preparing either (opening the PR, writing the exact commands, verifying preconditions, dry-running
where a dry-run path exists) is in scope. Executing them is not.

Everything else — dev, staging, all merges to `dev`, all grooming, all tactical and product calls
in that domain — is the proxy's, without checking back.

## Decision log

Every entry: what was decided, why, and **what would falsify it**. Own errors recorded explicitly.

### D1 — the owner is represented by the product-owner agent for this run
**Decided by:** owner, 2026-07-28, before the run began; boundary amended by the owner the same
day after the first draft was too broad.
**Effect:** decisions route to `product-owner` and the run continues on its answer, rather than
blocking. Logged entries name the proxy as the decider, not the owner personally, so a later
reader can tell a delegated call from one the owner made.
**Boundary, final:** only two things stop the run — `dev -> main` (#236) and mutating production
DATA (#113). Reversible production config (#235) is the proxy's. The test is reversibility, not
environment.
**Falsified if:** a proxy decision contradicts a standing owner decision recorded elsewhere (the
standing decision wins and the conflict is reported), or if an action the proxy authorised turns
out to be irreversible in a way this boundary did not anticipate — in which case stop and report
rather than reasoning by analogy.

### D2 — #235 is executable by this run; #113 is not
**Decided by:** owner, 2026-07-28.
**#235** (30-day session TTL on prod): the proxy may authorise. Add
`MDREVIEW_SESSION_TTL_S: "${MDREVIEW_SESSION_TTL_S:-2592000}"` to the host's
`docker-compose.prod.yml` environment block, recreate, then **verify with
`docker exec mdreview printenv MDREVIEW_SESSION_TTL_S`** — an `.env`-only change is silently inert
because neither compose service has an `env_file:` directive, and the recreate reports success
either way. Existing sessions survive; the signing secret persists in `.env`.
**#113** (reconcile the live prod `owner=''` corpus): **prepare only.** Verify preconditions,
confirm whether `reconcile.py` has a dry-run/confirm path, write the exact command, and stop.
**Why the split:** reversibility. #235 is undone by one line; #113 stamps ownership onto real user
documents and is not undoable. That exact failure is #97.
**Falsified if:** #235 turns out to affect more than the session lifetime, or if `reconcile.py`
has no confirm path at all, which would make even preparing it hazardous and is itself a finding.

### D3 — sprint-35 is NO-GO as composed; the run proceeds on the #216 -> #214 subset only
**Decided by:** agent, on the ticket-groomer's verdict, 2026-07-28.
**Why:** 4 of 6 tickets cannot be executed by an autonomous run. Three act on production
(#236 adoption, #235 host compose, #113 live data) and the restricted agent account has neither
`docker exec` nor a compose verb — verified, not assumed: `agent.sudoers` grants only
`agent-logs`, `agent-status`, `agent-drift`, and `ssh kapture-agent drift-check` returns
"sudo: a password is required". #215 needs a decision because its AC2 would document a host verb
that does not exist.
**What was NOT done:** no acceptance criterion was weakened to make anything runnable. Four
tickets moved to `status:blocked` with a "do not re-ready at stage 1" note, because stage 1's exit
criterion IS `status:ready` and a label swap alone would let a later firing re-ready them.
**Falsified if:** the agent account gains a deploy verb, or the owner executes the prod actions
themselves, at which point #235 and #113 become gradeable again.

### D4 — the sprint's stated priority order is inverted relative to executability
**Decided by:** agent, 2026-07-28.
**Corrected order: #216 then #214.** #215 and #216 both edit `docs/process/autonomous-run.md`, so
#216 goes first (smaller, self-contained, and #215 may be re-scoped); #215 would rebase. #214
touches disjoint files and is safe alongside.
**Falsified if:** #215 is re-scoped to not touch `autonomous-run.md`, removing the contention.

### E1 — I told the owner prod was still on the 12-hour default; they had already fixed the cause
**What happened:** I reported #236 as outstanding work and sequenced a whole sprint around
promoting `dev -> main`. The owner promoted it themselves at 21:15Z while we were still talking.
My sprint-35 composition was stale within minutes of being written.
**Not an error in the plan so much as in its shelf life:** a sprint whose top item is an owner
action can be invalidated by the owner doing it. Worth noting for future sprint composition:
owner-gated items should be tracked, not scheduled as sprint positions.


### D5 — #183: #222's single-key bindings SURVIVE; ⌘K is additive
**Decided by:** product-owner (owner proxy), 2026-07-28.
**Why:** the design doc §10 rule 01 ("44px minimum hit target — rows, filter words and the primary
button all clear it on touch") shows the filter words are visible and tappable in the narrow
design, so rule 02's "it replaces search, filters and project switching" is about MOBILE CHROME,
not keyboard dispatch. The doc predates #222 and says nothing about keys.
**Falsified if:** the owner wants the palette to own those keys, or `keys.js` cannot hold `mod+k`
alongside single-key bindings (that would be a keys.js bug to file, not a licence to delete
shipped bindings).

### D6 — #184: reduced motion AND `cdp-shot --media` descoped; narrow widths only
**Decided by:** product-owner (owner proxy), 2026-07-28.
**Why:** §10 rule 04 names its subjects — "shimmer and spinners fall back to a static tint" — and
both left the programme by the owner's own earlier decisions (skeletons are not in the approved
plan's slice 1; the "agent working" ring belonged to the turn bar, which is #185, closed
NOT_PLANNED today). Verified across every first-party web/app file on 5f2c2c0: ZERO @keyframes,
ZERO `animation:`, ZERO prefers-reduced-motion. Nothing in this app moves, so the AC "verified by
computed animationName stepping" PASSED TODAY UNCHANGED — a vacuous criterion.
**Replacement, not a dropped requirement:** a standing rule is posted on epic #152 — any ticket
introducing motion ships its reduced-motion fallback in the same change.
**Falsified if:** a shimmer or spinner lands on the dashboard before #184 ships.

### D7 — #213 retitled to name latex-viewer.html
**Decided by:** product-owner (owner proxy), 2026-07-28.
**Why:** stage 3's contended-file check reads the ticket; a title naming `viewer.html` would have
falsely serialised #213 behind #199. Real contention is #205.
**Falsified if:** the fix turns out to need a viewer.html edit.

### D8 — #183 and #184 moved to sprint-37, which is NOT opened
**Decided by:** product-owner (owner proxy), 2026-07-28.
**Why:** sprint-36's goal enumerates five truth-telling clauses that map 1:1 onto
#199 #205 #213 #224 #206; the other two serve none of them. sprint-37 exists as a container only;
G6 activation is the owner's.
**Consequence for this run:** it ENDS after #206. Do not claim tickets from an unopened milestone.

### E2 — I groomed #199 wrongly this morning, twice, and the groomer caught both
**What happened:** (a) I asserted the rail docks at "1280x800 and 1366x768". Measured: at 1366 the
gate is already satisfied (1366 >= the 1312 threshold), so that acceptance criterion would have
PASSED BEFORE A LINE WAS WRITTEN. The real band is ~1100-1311. (b) I DROPPED the original AC3
(#pop stays 284px beside its text), which is the only criterion that catches the second call site.
**Why it matters:** `layoutComments()` and `positionPop()` carried the identical threshold. With
AC3 dropped, a fix to one would have shipped a viewer whose rail sits beside the text while its
composer docks, and every remaining criterion would still have been green.
**This is the failure the no-weakening rule names**, committed by me during grooming rather than
during implementation, which is exactly where it is hardest to see.
**Falsified if:** the dropped AC had been redundant with another — it was not; nothing else in the
ticket referenced positionPop.

### E3 — my own check was flaky four different ways before it was trustworthy
Multi-line `--eval` broke output parsing; a fixed port let one run measure another run's leftover
server (failures with exit 0); five Chrome launches per run contended and hit the async-render trap
(hard rule 5); a helper was defined after first use (12/12 failures). Now 12/12 clean, retrying
only on ZERO measurements — infrastructure — never on a failed assertion.
**Worth keeping because:** three of the four were mine, and each produced a *plausible* failure
that could have been blamed on the product.


### D9 — #199 stays parked at stage 8; headless geometry is NOT accepted as substitute evidence
**Decided by:** product-owner (owner proxy), 2026-07-29.
**The argument I made and lost:** the owner's no-headless rule targets SYNTHETIC INPUT (a fabricated
KeyboardEvent), whereas #199's criteria are viewport GEOMETRY, and CDP's setDeviceMetricsOverride is
a real relayout, not a simulation. The proxy rejected it on the ticket's own record: #199's body drew
that exact line BEFORE it became inconvenient ("no headless screenshot substitutes for this"), so
accepting it now is hard rule 7's "a criterion reworded after the fact". Nothing learned tonight was
new information; only that it turned out to be hard.
**Falsified if:** the owner rules that CDP geometry counts (then the park was over-cautious), or any
real-Chrome route reaches all three widths (which kills the park's premise outright).

### E4 — I declared a capability impossible after testing one approach
**What happened:** I reported "the extension cannot set viewport width" on the strength of two
resize attempts against ONE window, and routed a decision on that premise. The proxy challenged it:
innerWidth 1512 == screen.availWidth while outerWidth == 756 is physically impossible for a normal
window, so the correct read was "this window ignores resize", not "the tool cannot resize".
**It was right.** Creating a NEW window, resizing it, THEN navigating produced innerWidth 1180
exactly, with the rail visibly beside the text and all three cards rendered — the decisive visual
evidence I had just told the owner was unobtainable.
**Lesson:** "I tried and it did not work" is a statement about my approach, not about the tool. A
capability claim needs the same evidence standard as a defect claim.
**Outcome, honestly:** 1 of 3 widths reached, so the park still stands under the proxy's own
"all three or parked" condition. I did not lift it on partial evidence.


### D10 — #206 held back while #224's stage 7 was pending
**Decided by:** agent, 2026-07-29.
**Why:** #206's PR was green while #224 was merged and awaiting adoption. Merging would have made
the next digest a build containing BOTH, and #224's watcher (already holding CI success for
748c6e7) would have claimed it. The image would genuinely contain #224's code, so the claim would
not have been false — but it would have been attribution by luck rather than by evidence, which is
the trap that produced the #222 near-miss earlier in this run.
**Cost:** one 15-minute timer cycle. **Falsified if:** waiting ever costs more than the ambiguity
it removes, e.g. a queue of tickets each blocking the next.

### E5 — two of three assertions in #206's check were VACUOUS
**What happened:** the self-check passed with the fence/inline order REVERSED and with a greedy
regex substituted. Both "tests" proved nothing.
**Why:** the ordering case used balanced inline spans, which strip clean either way; the stray-tick
case used a SINGLE backtick, and a greedy `.*` finds no pair with one tick so it strips nothing and
passes.
**Fixed:** the greedy case now uses two separate spans around a real preamble and genuinely fails.
**Not fixed, reported instead:** the ordering criterion is UNFALSIFIABLE as written —
_strip_inline_code pairs backtick runs with each other and a ``` delimiter IS such a run, so
inline-first subsumes fence stripping on every shape tried. Filed on #206 rather than left as a
test that passes regardless.
**Lesson:** mutation-testing is not a formality. Without it, this ticket would have shipped with a
check that could not fail.

<!-- Further entries are added during the run. -->

## Stop conditions

- The groomer returns NO-GO, or a decision blocks a ticket others depend on.
- A decision falls in the boundary class above (main / prod data / prod config).
- Any stage needs a judgement this plan did not anticipate **and** the product-owner agent declines
  it or is uncertain: stop and report, do not decide.
- A precondition that cannot be evaluated stops the run. Unevaluable is not true.
- If the run stops after stage 6 but before stage 7 completes, `dev` carries code that never
  reached staging. Say so on both the PR and the issue.
