# Run: sprint-38

Process: `docs/process/autonomous-run.md`. Epic / evidence home: #255.
Owner opened the sprint (G6) 2026-07-29 and authorised the run.

## CURRENT POSITION — read this first, update it last

```
sprint:    38 — scope GREW during the run (owner added 4). 1 done, 2 merged-awaiting-adoption.
ticket:    #262 next to build (groomed GO, size L, the sprint's must-ship)
stage:     #254 DONE status:review (route found + corrected twice; precondition CONFIRMED).
           #243 merged 15c0ab7, stage-7 watcher running (pre 14b2396f).
           #241 built, PR #264 open, HELD until #243 adopts (D3, one per window).
           #257 ready, untouched. #261 NEEDS-DECISION -> resolved by owner: split.
           #262 GROOMED GO. #265 filed (full-screen palette bug, split from #261).
next:      when #243 adopts -> merge #264 (#241) -> its stage 7/8/9.
           Build #262 in parallel (zero contention with docs tickets).
           Order after that: #262 -> #261 -> #265 (real contention is theme.css + dashboard.html).
branch:    (worktrees live: wt-254 wt-243 wt-241; wt-262 to create)

SPRINT SCOPE NOW (owner added #257 #261 #262 mid-run; #265 split out):
  #254 done · #243 merged · #241 PR open · #257 ready · #262 ready(L) · #261 ready-after-split · #265 ready
  Sprint is NOT done until all of these ship. Owner said so explicitly.

MY OWN DEFECTS FOUND BY GROOMERS THIS RUN (all from #183, which I wrote):
  1. `--accent-soft` does not exist -> the keyboard-selected palette row is INVISIBLE in both
     themes (measured rgba(0,0,0,0) selected AND unselected). Real token is --accent-bg.
     Fixed inside #261.
  2. The <=720px full-screen palette NEVER RENDERED: the media block styles .command-dialog
     (transparent wrapper) instead of .command-dialog > .command (the visible panel). Measured
     574x176 at 606px. Split to #265.
  3. THE CHECK THAT HID IT: keys_selfcheck's "palette goes full-screen" case is a regex over CSS
     TEXT. Green since #183 merged while the outcome was never full-screen. I mutation-tested
     whether the check noticed the TEXT changing, never whether the CSS produced the RESULT.
     Same class as #222's constructed KeyboardEvent. THIS IS THE LESSON OF THE RUN: assert
     rendered geometry, not the presence of a declaration.

FOUR WRONG CLAIMS IN MY #262 BODY (groomer caught each; all would have shipped a defect):
  - "three states" is FOUR (the else branch renders a Sign in link)
  - the --brand swap is a REGRESSION: #fff on --brand is 2.18:1 in dark vs 5.31:1 today
  - "no owner column" is FALSE: created_by exists (conclusion survives, better reason)
  - "revoke is CSRF-checked" is HALF TRUE -> filed #266

FILED THIS RUN: #257 #261 #262 #265 #266 (prod CSRF gap, P2) #267 (raw uid in viewers, P3)

NON-OBVIOUS CONSTRAINTS FOR #262, do not rediscover:
  - reduced-motion block goes in theme.css, NOT dashboard.html: account.js injects its <style>
    AFTER theme.css's <link>, so a guard there cannot override the injected transition. The
    menu's transition must live in theme.css too or the guard silently does nothing.
  - HAND-ROLL the dropdown. Basecoat is on 3 of 5 mount points; viewer + latex-viewer load
    neither its CSS nor its JS. Basecoat is never the primary, so there is no fallback to design.
  - list_named returns opaque user:<provider:sub>. "Shared with by name" needs the new
    UserService.email_for(uid) with an honest fallback.
driver: CronCreate b4c85eac. prod HANDS OFF. dev->main owner-only (#236, 156 commits).
```

## Decision log

### D1 — sprint-38 opened by the owner, floor waived at three
**Decided by:** owner, 2026-07-29: "open sprint-38", after accepting the product owner's
recommendation to ship 3 rather than pad to 5. Precedent: his own correction to sprint-36.
#187 was groomed ready but deliberately held for sprint-39, because its ACs need the real-browser
verification this sprint exists to repair — it is downstream of the sprint, not a peer.
**Falsified if:** three tickets turn out to leave the sprint without a coherent goal.


### D2 — #257 (README shrink) added to sprint-38 mid-run by the owner
**Decided by:** owner, 2026-07-29: "log another ticket about making the readme.md short and
concise. It looks like AI slop atm. and make it part of the sprint."
Measured before filing: 544 lines, 5,578 words, 39,404 bytes, 81 em dashes. The usual slop markers
are ABSENT (zero emoji, zero bold lead-in bullets, zero "crucial/seamless/leverage"), so the defect
is SCOPE, not vocabulary: it is an operator manual in the front door. ACs are cut-to-150-lines,
zero em dashes, and move-don't-delete every runbook into docs/ with links.
**Falsified if:** the owner wanted a tone rewrite rather than a structural cut.

### E1 — I reported "the extension cannot deliver key presses" too early (#254)
Single tool calls delivered ZERO keydown events, and I filed #254 and parked #183 on that basis.
Both were correct as far as they went, but the conclusion was too broad: batching the actions in
ONE browser_batch round trip DOES deliver real keys, reproducibly (3/3), and with it I verified
every criterion that had been parked. The park was honest; the diagnosis was incomplete. Same
lesson as 2026-07-28's viewport claim: "I tried and it did not work" is a statement about my
approach, not about the tool.


### E2 — I documented a route as "4/4 reproducible" and it failed 3/3 on the next window
The successes were real (palette opened, focus moved to #cmdq, ? sheet rendered all seven
bindings). But they were a burst inside one window state I cannot reproduce on demand, and I wrote
the doc as though the recipe were reliable. Stage 8 for this very ticket is what caught it: the
rule "docs-only tickets still need their claims exercised" did exactly the work it exists to do.
Corrected in 571aacb to necessary-but-not-sufficient, intermittent, bounded retry then park.
**Falsified if:** the OS-frontmost hypothesis is confirmed and the route becomes deterministic
given that precondition — in which case the doc should state the precondition, not the shrug.


### E3 — I shipped a vacuous check in #183 and mutation-tested it wrong
The "palette goes full-screen at <=720px" case greps the CSS TEXT for `100vw`. It has been green
since #183 merged, while the rendered panel was 574x176 at 606px and never full-screen, because
the media block styles the transparent wrapper instead of the visible panel. My mutation tests
proved the check noticed the CSS TEXT changing; they never proved the CSS produced the intended
RESULT. That is the same defect class as #222's constructed KeyboardEvent, committed by me on the
same day I wrote the rule against it. Split to #265, whose AC forbids a text-regex check.
**Falsified if:** a text assertion can be shown to imply the rendered outcome — it cannot here,
which is the whole point.

### D3 — #261 split rather than grown (owner, 2026-07-29)
The groomer surfaced that fixing the full-screen palette turns a polish ticket into a responsive
layout ticket mid-run. Owner chose: keep #261 as motion + polish + the invisible-selection fix,
file #265 for the layout. Rationale accepted: #265 also wants #243's viewport route, which is
still in flight.
**Falsified if:** #261's entrance animation and #265's layout turn out to need designing together
after all, in which case #265 rebases on #261 rather than the reverse.

<!-- Further entries are added during the run. -->
