# Claude SUBSCRIPTION OAuth token for the containerized watcher's agent.
# Mint once on a machine where you're logged in:   claude setup-token
#   (requires a Claude subscription — this is NOT an API key, no per-token billing)
# Copy this file to .env and paste the value below. .env is gitignored; never commit a token.
CLAUDE_CODE_OAUTH_TOKEN=
