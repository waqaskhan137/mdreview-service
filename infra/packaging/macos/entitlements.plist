<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Hardened-runtime entitlements for the notarized macOS .app (P3).
     The bundle embeds its own Python framework + many .so/.dylib; these two let the embedded
     interpreter load them under the hardened runtime. Both are notarization-allowed. Tighten on the
     first successful notarized run if either proves unnecessary. -->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
